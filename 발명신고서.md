# 발명신고서

> **작성일**: 2026. 01. 23.
> **과제명**: Cortex - 데이터 기반 투자 전략 플랫폼

---

## 1. 기본 정보

| 항목            | 내용                                                                                                                                                                |
| --------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **발명의 명칭** | 머신러닝 모델 기반 투자 전략 로직 블록 통합 시스템 및 방법<br>(System and Method for Integrating Investment Strategy Logic Blocks Based on Machine Learning Models) |
| **발명자**      | (본인 성명 기재)                                                                                                                                                    |

---

## 2. 관련 특허 및 선행기술

### 관련 선행기술 1

- **명칭**: 퀀트커넥트 (QuantConnect)
- **내용**: 알고리즘 트레이딩 플랫폼으로 파이썬/C# 코드를 통해 투자 전략을 작성하고 백테스팅 제공
- **한계점**: 사용자가 프로그래밍 언어를 알아야 하며, AI 모델을 전략에 통합하려면 복잡한 코딩이 필요함. 노코드 환경에서의 유동적 모델 결합이 어려움.

### 관련 선행기술 2

- **명칭**: 트레이딩뷰 (TradingView)
- **내용**: 파인스크립트(Pine Script)를 이용한 지표 생성 및 전략 테스트
- **한계점**: 기술적 지표 중심이며, 사용자가 직접 학습시킨 딥러닝 모델(LSTM 등)을 차트 위에서 로직 블록처럼 활용하는 기능은 제공하지 않음.

---

## 3. 발명 내용

### 1. 발명 소개

**요약**:
본 발명은 사용자가 직접 학습시킨 인공지능(AI) 예측 모델을 일반 기술적 보조지표와 동등한 **'로직 블록(Logic Block)'**으로 추상화하여, 노코드(No-Code) 인터페이스에서 드래그 앤 드롭 방식으로 복합 투자 전략을 구성할 수 있게 하는 시스템이다. 특히 분류(Classification) 및 회귀(Regression) 모델의 예측 결과를 4가지 평가 모드(최고확률, 임계값, 방향성, 신뢰도)로 해석하여 매매 신호로 변환하는 신호 평가 엔진을 포함한다.

### 2. 종래기술 및 문제점

**기존 기술**:
기존의 알고리즘 트레이딩 플랫폼은 두 가지로 양분되어 있다.

1. **코드 기반 플랫폼**: AI 모델 활용은 가능하나 높은 프로그래밍 지식 요구 (진입장벽 높음)
2. **노코드/GUI 플랫폼**: 기술적 지표(RSI, MACD 등) 조합은 쉬우나, 사용자 정의 AI 모델을 통합하는 기능 부재

**문제점**:
일반 투자자는 AI 기술을 활용하고 싶어도 코딩 장벽 때문에 접근이 어렵고, 반대로 노코드 툴 사용자는 고정된 지표만 사용할 수 있어 고도화된 전략 수립에 한계가 있다. 또한 AI 모델의 예측값(확률, 연속값)을 단순 매매 신호(Buy/Sell)로 변환하는 명확한 기준 설정 인터페이스가 부족하다.

### 3. 본 발명의 목적

1. **AI의 민주화**: 코딩 없이도 복잡한 딥러닝 모델(LSTM, GRU, Transformer 등)을 투자 전략의 구성 요소로 활용 가능하게 함
2. **복합 전략 구성**: "RSI가 30 이하이면서(AND) AI 모델이 상승을 예측할 때"와 같은 하이브리드 전략을 시각적으로 구성
3. **불확실성 제어**: MC Dropout 등을 활용한 모델의 신뢰도(Confidence)를 매매 조건으로 설정하여 리스크 관리

### 4. 본 발명의 효과

1. **전략 다양성 증대**: 기술적 분석과 AI 예측을 결합하여 기존에 없던 독창적인 알파(Alpha) 창출 가능
2. **사용성 극대화**: AI 모델의 복잡한 추론 과정을 '블록' 하나로 단순화하여 사용자 편의성 제공
3. **검증된 투자**: 백테스팅 엔진과 연동되어 AI 전략의 과거 성과를 즉시 검증 가능

### 5. 목적을 달성하기 위한 수단

**핵심 구성 요소**:

1. **AI 로직 블록 정의부**: AI 모델의 메타데이터(입력 형태, 출력 타입)를 분석하여 표준화된 인터페이스의 로직 블록으로 변환
2. **신호 평가 엔진 (AI Signal Evaluator)**: AI 모델의 출력을 불리언(True/False) 조건으로 변환
3. **비주얼 전략 빌더**: 블록 간의 논리 연산(AND/OR) 및 계층적 결합을 지원하는 GUI

**구현 방법**:

- **블록 타입 통합**: `AISignalLogic` 타입을 기존 `LogicBlock` 유니온 타입에 포함시켜 기술적 지표와 동일 계층 처리
- **평가 모드 다변화**:
  - **전용 모델**: 분류(Buy/Sell) vs 회귀(가격 예측) 자동 인식
  - **평가 모드**: Direction(방향), Threshold(임계값), Confidence(신뢰구간), Highest(최대확률)

---

## 4. 상세 설명

### 1. 시스템 구성도

```mermaid
graph TD
    User[사용자] -->|학습 데이터 설정| AILab[AI Lab / 학습 엔진]
    AILab -->|모델 생성 (ONNX)| ModelRepo[AI 모델 저장소]

    User -->|전략 구성 (Drag & Drop)| Builder[비주얼 전략 빌더]
    ModelRepo -->|모델 블록화| Builder
    Indicators[기술적 지표 라이브러리] -->|지표 블록| Builder

    Builder -->|전략 JSON 생성| Engine[백테스팅/실행 엔진]

    subgraph "Signal Evaluator Engine"
        Engine -->|실시간 데이터 주입| Inference[추론 모듈 (ONNX Runtime)]
        Inference -->|예측값 (확률/수치)| Evaluator[신호 평가기]
        Evaluator -->|매매 신호 (True/False)| StrategyLogic[전략 로직 처리]
    end
```

### 2. 단계별 동작 흐름

1. **모델 학습**: 사용자가 종목/기간을 설정하여 AI 모델 학습 (Triple Barrier Labeling 적용)
2. **블록 선택**: 전략 빌더의 'Indicator Hub'에서 학습된 AI 모델 선택
3. **조건 설정**:
   - 예: "Bitocin Price Predictor(AI)" 모델 선택
   - 모드: **Confidence Mode** (신뢰도 기반)
   - 조건: **95% 신뢰구간 하한 > 0** (확실한 상승 예측 시에만 진입)
4. **전략 실행**: 백테스팅 엔진이 과거 데이터에 대해 추론을 수행하고 수익률 검증

### 3. 세부 기능 명세: 다양한 평가 모드 (Evaluation Modes)

본 발명의 핵심 기술인 **AI 신호 평가기(AISignalEvaluator)**는 다음의 4가지 모드를 제공한다:

| 모드           | 대상 모델             | 동작 원리                             | 활용 예시                                        |
| -------------- | --------------------- | ------------------------------------- | ------------------------------------------------ |
| **Highest**    | 분류 (Classification) | 예측 확률이 가장 높은 클래스 선택     | "상승 확률이 하락/보합보다 높으면 매수"          |
| **Threshold**  | 분류/회귀             | 예측값/확률이 특정 임계값 초과 시     | "상승 확률이 70% 이상일 때만 매수"               |
| **Direction**  | 회귀 (Regression)     | 예측값의 부호(+, -)에 따라 판단       | "가격 변동 예측값이 양수(+)면 매수"              |
| **Confidence** | 회귀 (Regression)     | **MC Dropout**을 통한 불확실성 필터링 | "예측 불확실성이 낮고 상승이 확실시될 때만 진입" |

이러한 모드들은 `RuleBlock.tsx` 컴포넌트를 통해 사용자에게 직관적인 UI로 제공되며, 백엔드의 `ai_signal_evaluator.py`에서 실제 연산이 수행된다.

---

## 5. 실험 내용 및 결과 활용 예

### 실제 적용 예시

**[ 하이브리드 돌파 전략 ]**

- **기존 방식**: 볼린저 밴드 상단 돌파 시 매수 (거짓 신호 많음)
- **본 발명 적용**:
  1. **조건 A (지표)**: 가격이 볼린저 밴드 상단 돌파
  2. **조건 B (AI)**: LSTM 모델의 상승 예측 확률 > 60%
  3. **결합**: 조건 A **AND** 조건 B 만족 시에만 매수

### 예상 결과

- 단순 지표 전략 대비 **승률(Win Rate) 15% 이상 향상** 기대
- MC Dropout을 통한 신뢰도 필터링 적용 시 **MDD(최대 낙폭) 감소** 효과
- 노코드 환경 제공으로 전략 개발 시간 **90% 단축**

## 6. 핵심 키워드

1. **노코드 전략 빌더 (No-Code Strategy Builder)**
2. **AI 로직 블록 (AI Logic Block)**
3. **신호 평가 엔진 (Signal Evaluation Engine)**
4. **하이브리드 트레이딩 (Hybrid Trading)**
5. **MC Dropout 불확실성 추정 (Uncertainty Estimation)**
